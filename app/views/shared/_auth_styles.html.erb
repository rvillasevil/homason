<style>
  .auth-page {
    min-height: calc(100vh - 140px);
    padding: clamp(4rem, 8vw, 6rem) 1.5rem;
    display: grid;
    gap: clamp(2rem, 4vw, 3rem);
    max-width: 1080px;
    margin: 0 auto;
  }

  @media (min-width: 1024px) {
    .auth-page {
      grid-template-columns: 1.1fr 0.9fr;
      align-items: stretch;
    }
  }

  .auth-card,
  .auth-side-panel {
    background: rgba(255, 255, 255, 0.92);
    border-radius: 1.75rem;
    padding: clamp(2rem, 4vw, 2.6rem);
    border: 1px solid rgba(102, 92, 170, 0.16);
    box-shadow: 0 30px 70px rgba(31, 26, 61, 0.12);
    display: grid;
    gap: 1.6rem;
  }

  .auth-card__header {
    display: grid;
    gap: 0.75rem;
  }

  .auth-card__eyebrow {
    font-size: 0.8rem;
    letter-spacing: 0.18em;
    text-transform: uppercase;
    font-weight: 700;
    color: var(--brand-primary);
  }

  .auth-card__subtitle {
    color: var(--text-soft);
    font-size: 1.02rem;
    line-height: 1.55;
  }

  .auth-form {
    display: grid;
    gap: 1.2rem;
  }

  .auth-form__group {
    display: grid;
    gap: 0.55rem;
  }

  .auth-form__group label {
    font-weight: 600;
    color: var(--text-strong);
  }

  .auth-form__hint {
    font-size: 0.85rem;
    color: var(--text-soft);
  }

  .auth-form__actions {
    display: flex;
    flex-wrap: wrap;
    gap: 0.75rem;
    align-items: center;
  }

  .auth-form__actions .btn {
    flex-shrink: 0;
  }

  .auth-link {
    color: var(--brand-primary);
    font-weight: 600;
  }

  .auth-side-panel {
    background: linear-gradient(135deg, rgba(76, 62, 237, 0.12), rgba(255, 186, 83, 0.1));
    border: 1px solid rgba(76, 62, 237, 0.22);
    position: relative;
    overflow: hidden;
  }

  .auth-side-panel::after {
    content: "";
    position: absolute;
    inset: auto -40% -40% auto;
    width: clamp(200px, 30vw, 280px);
    aspect-ratio: 1 / 1;
    background: radial-gradient(circle at 30% 30%, rgba(76, 62, 237, 0.25), transparent 65%),
      radial-gradient(circle at 70% 60%, rgba(255, 186, 83, 0.25), transparent 65%);
    filter: blur(0px);
    opacity: 0.9;
  }

  .auth-side-panel__title {
    font-size: clamp(1.6rem, 3vw, 2rem);
    color: var(--text-strong);
  }

  .auth-benefits {
    display: grid;
    gap: 1rem;
  }

  .auth-benefits li {
    list-style: none;
    padding: 1rem 1.2rem;
    border-radius: 1.2rem;
    background: rgba(255, 255, 255, 0.9);
    border: 1px solid rgba(102, 92, 170, 0.12);
    box-shadow: inset 0 1px 0 rgba(255, 255, 255, 0.6);
    font-weight: 500;
    color: var(--text-medium);
    display: grid;
    gap: 0.3rem;
  }

  .auth-benefits li strong {
    color: var(--text-strong);
  }

  .auth-meta {
    display: grid;
    gap: 0.4rem;
    font-size: 0.92rem;
    color: var(--text-soft);
  }

  .auth-meta strong {
    color: var(--text-strong);
  }

  .auth-checkbox {
    display: flex;
    gap: 0.6rem;
    align-items: flex-start;
    font-size: 0.85rem;
    color: var(--text-soft);
  }

  .auth-checkbox input[type="checkbox"] {
    margin-top: 0.2rem;
    width: 1.05rem;
    height: 1.05rem;
    border-radius: 0.4rem;
  }

  .auth-form__footer {
    font-size: 0.9rem;
    color: var(--text-soft);
  }
</style>
