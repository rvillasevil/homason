<%= render "shared/auth_styles" %>

<section class="auth-page">
  <div class="auth-card">
    <div class="auth-card__header">
      <span class="auth-card__eyebrow">Clientes Homason</span>
      <h1>Accede a tus reformas y reservas</h1>
      <p class="auth-card__subtitle">
        Inicia sesión para seguir el avance de tus obras, revisar pagos programados y coordinar entregas de materiales con tu project manager.
      </p>
    </div>

    <%= form_with url: login_path, method: :post, local: true, html: { class: "auth-form" } do |form| %>
      <div class="auth-form__group">
        <%= form.label :email, "Correo electrónico" %>
        <%= form.email_field :email, required: true, autocomplete: "email" %>
      </div>

      <div class="auth-form__group">
        <%= form.label :password, "Contraseña" %>
        <%= form.password_field :password, required: true, autocomplete: "current-password" %>
      </div>

      <%= form.hidden_field :role, value: "customer" %>

      <div class="auth-form__actions">
        <%= form.submit "Entrar en mi cuenta", class: "btn btn--primary" %>
        <span>¿Aún no eres cliente? <a class="auth-link" href="<%= clients_signup_path %>">Reserva tu primera obra</a></span>
      </div>
    <% end %>

    <div class="auth-form__footer">
      Si tienes problemas para acceder, escríbenos a <a class="auth-link" href="mailto:soporte@homason.com">soporte@homason.com</a> con el asunto “Acceso clientes”.
    </div>
  </div>

  <aside class="auth-side-panel">
    <h2 class="auth-side-panel__title">Controla cada fase de tu reforma</h2>
    <ul class="auth-benefits">
      <li>
        <strong>Seguimiento diario</strong>
        Consulta hitos, fotos y comentarios de la obra compartidos por tu equipo Homason.
      </li>
      <li>
        <strong>Pagos transparentes</strong>
        Revisa cronogramas de facturación, descarga comprobantes y solicita cambios si es necesario.
      </li>
      <li>
        <strong>Gestión de materiales</strong>
        Aprueba pedidos, confirma entregas y recibe alertas cuando haya incidencias.
      </li>
    </ul>
    <div class="auth-meta">
      <strong>¿Necesitas asistencia inmediata?</strong>
      Contacta con tu project manager o llama al <a class="auth-link" href="tel:+34911223344">+34 911 223 344</a>.
    </div>
  </aside>
</section>
