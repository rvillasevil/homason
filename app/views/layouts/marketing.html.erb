<!DOCTYPE html>
<html lang="es">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Homason · Reformas orquestadas con la precisión de una scale-up</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=DM+Sans:wght@400;500;600;700&family=Instrument+Serif:ital@0;1&display=swap"
      rel="stylesheet"
    />
    <style>
      :root {
        --brand-primary: #4c3eed;
        --brand-secondary: #735bff;
        --brand-highlight: #ffba53;
        --brand-soft: #f5f3ff;
        --surface: #ffffff;
        --surface-alt: #f8f7ff;
        --text-strong: #1f1a3d;
        --text-medium: #4a4763;
        --text-soft: #6d6a85;
        --border: rgba(102, 92, 170, 0.18);
        --shadow-lg: 0 40px 80px rgba(76, 62, 237, 0.18);
        --shadow-md: 0 20px 50px rgba(31, 26, 61, 0.08);
        --radius-xl: 2.5rem;
        --radius-lg: 1.75rem;
        --radius-md: 1.25rem;
        --content-max: 1180px;
      }

      *,
      *::before,
      *::after {
        box-sizing: border-box;
      }

      body {
        margin: 0;
        font-family: "DM Sans", system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
        color: var(--text-medium);
        background: linear-gradient(180deg, #ffffff 0%, #f3f1ff 100%);
        line-height: 1.6;
        font-size: 17px;
      }

      h1,
      h2,
      h3,
      h4,
      h5,
      h6 {
        color: var(--text-strong);
        margin: 0;
        letter-spacing: -0.02em;
      }

      h1 {
        font-size: clamp(2.6rem, 5vw, 3.7rem);
        line-height: 1.05;
      }

      h2 {
        font-size: clamp(2rem, 4vw, 2.7rem);
        line-height: 1.15;
      }

      h3 {
        font-size: clamp(1.35rem, 3vw, 1.7rem);
      }

      p {
        margin: 0;
      }

      a {
        color: inherit;
        text-decoration: none;
      }

      a:hover,
      a:focus {
        color: var(--brand-primary);
      }

      .page {
        min-height: 100vh;
        display: flex;
        flex-direction: column;
      }

      main {
        flex: 1;
      }

      .navbar {
        position: sticky;
        top: 0;
        z-index: 30;
        backdrop-filter: blur(16px);
        background: rgba(255, 255, 255, 0.88);
        border-bottom: 1px solid rgba(255, 255, 255, 0.5);
      }

      .navbar__inner {
        max-width: var(--content-max);
        margin: 0 auto;
        padding: 1.2rem 1.5rem;
        display: flex;
        align-items: center;
        justify-content: space-between;
        gap: 1.5rem;
      }

      .navbar__brand {
        display: inline-flex;
        align-items: center;
        gap: 0.65rem;
        font-weight: 700;
        font-size: 1.2rem;
        color: var(--brand-primary);
      }

      .navbar__brand-mark {
        width: 42px;
        height: 42px;
        border-radius: 14px;
        background: linear-gradient(140deg, var(--brand-primary), var(--brand-secondary));
        display: grid;
        place-items: center;
        color: #fff;
        font-weight: 700;
      }

      .navbar__menu {
        display: flex;
        align-items: center;
        gap: 1.5rem;
        list-style: none;
        padding: 0;
        margin: 0;
        font-weight: 500;
      }

      .navbar__menu a {
        position: relative;
        padding-bottom: 0.2rem;
      }

      .navbar__menu a::after {
        content: "";
        position: absolute;
        inset-inline: 0;
        bottom: -0.2rem;
        height: 2px;
        background: linear-gradient(120deg, var(--brand-primary), var(--brand-secondary));
        transform: scaleX(0);
        transform-origin: center;
        transition: transform 150ms ease;
      }

      .navbar__menu a:focus::after,
      .navbar__menu a:hover::after {
        transform: scaleX(1);
      }

      .navbar__actions {
        display: flex;
        align-items: center;
        justify-content: flex-end;
        flex-wrap: wrap;
        gap: 0.65rem;
        margin-left: auto;
      }

      .navbar__actions .btn {
        font-size: 0.9rem;
        padding: 0.65rem 1.4rem;
      }

      @media (max-width: 900px) {
        .navbar__menu {
          display: none;
        }

        .navbar__actions {
          width: 100%;
          justify-content: center;
          margin-left: 0;
        }
      }

      @media (max-width: 720px) {
        .navbar__inner {
          flex-wrap: wrap;
          justify-content: center;
        }

        .navbar__actions .btn {
          width: 100%;
          justify-content: center;
        }
      }

      .btn {
        display: inline-flex;
        align-items: center;
        justify-content: center;
        border-radius: 999px;
        border: none;
        font-weight: 600;
        font-family: inherit;
        padding: 0.85rem 1.8rem;
        cursor: pointer;
        transition: transform 120ms ease, box-shadow 120ms ease;
        text-align: center;
        gap: 0.35rem;
      }

      .btn--primary {
        background: linear-gradient(120deg, var(--brand-primary), var(--brand-secondary));
        color: #fff;
        box-shadow: 0 22px 45px rgba(76, 62, 237, 0.35);
      }

      .btn--outline {
        background: rgba(255, 255, 255, 0.9);
        color: var(--brand-primary);
        border: 1px solid rgba(76, 62, 237, 0.35);
      }

      .btn--ghost {
        background: rgba(248, 247, 255, 0.9);
        color: var(--text-strong);
        border: 1px solid rgba(110, 102, 170, 0.12);
      }

      .btn:focus,
      .btn:hover {
        transform: translateY(-2px);
      }

      .section {
        padding: clamp(3.5rem, 7vw, 6.5rem) 1.5rem;
        position: relative;
      }

      .section--soft {
        background: var(--surface-alt);
      }

      .section--contrast {
        background: linear-gradient(135deg, #211c4f, #3e2fbf);
        color: rgba(255, 255, 255, 0.9);
      }

      .section__inner {
        max-width: var(--content-max);
        margin: 0 auto;
        display: grid;
        gap: clamp(2rem, 4vw, 3.5rem);
      }

      .section__eyebrow {
        font-size: 0.85rem;
        letter-spacing: 0.18em;
        text-transform: uppercase;
        font-weight: 700;
        color: var(--brand-primary);
      }

      .section--contrast .section__eyebrow {
        color: var(--brand-highlight);
      }

      .split {
        display: grid;
        gap: clamp(2rem, 4vw, 3.5rem);
        align-items: center;
      }

      @media (min-width: 920px) {
        .split {
          grid-template-columns: repeat(2, minmax(0, 1fr));
        }
      }

      .hero {
        padding-top: clamp(5rem, 10vw, 8rem);
      }

      .hero__inner {
        position: relative;
      }

      .hero__inner::after {
        content: "";
        position: absolute;
        inset: -10% auto auto -10%;
        width: clamp(260px, 50vw, 420px);
        aspect-ratio: 1 / 1;
        background: radial-gradient(circle at 30% 30%, rgba(255, 186, 83, 0.45), transparent 65%),
          radial-gradient(circle at 70% 70%, rgba(76, 62, 237, 0.28), transparent 65%);
        filter: blur(0px);
        z-index: -1;
      }

      .hero__content {
        display: grid;
        gap: 1.75rem;
      }

      .hero__meta {
        display: flex;
        flex-wrap: wrap;
        gap: 1rem 2.5rem;
        color: var(--text-soft);
      }

      .hero__meta strong {
        color: var(--text-strong);
        font-size: 1.1rem;
      }

      .hero__aside {
        border-radius: var(--radius-xl);
        background: var(--surface);
        padding: clamp(2rem, 4vw, 2.5rem);
        box-shadow: var(--shadow-lg);
        border: 1px solid rgba(255, 255, 255, 0.6);
        display: grid;
        gap: 1rem;
      }

      .hero__aside h2 {
        font-size: 1.4rem;
      }

      label span {
        display: block;
        font-weight: 600;
        margin-bottom: 0.35rem;
        color: var(--text-strong);
      }

      input,
      textarea,
      select {
        width: 100%;
        border-radius: 0.95rem;
        border: 1px solid var(--border);
        padding: 0.9rem 1rem;
        font: inherit;
        background: rgba(255, 255, 255, 0.92);
        box-shadow: inset 0 1px 2px rgba(31, 26, 61, 0.05);
      }

      textarea {
        resize: vertical;
      }

      input:focus,
      textarea:focus,
      select:focus {
        outline: 2px solid rgba(76, 62, 237, 0.28);
        border-color: rgba(76, 62, 237, 0.5);
      }

      .stack {
        display: grid;
        gap: 1.3rem;
      }

      .card {
        background: var(--surface);
        border-radius: var(--radius-lg);
        padding: clamp(1.8rem, 4vw, 2.4rem);
        border: 1px solid var(--border);
        box-shadow: var(--shadow-md);
        display: grid;
        gap: 1rem;
      }

      .card--accent {
        background: linear-gradient(135deg, rgba(76, 62, 237, 0.08), rgba(255, 186, 83, 0.08));
        border: 1px solid rgba(76, 62, 237, 0.22);
      }

      .card-grid {
        display: grid;
        gap: 1.5rem;
      }

      @media (min-width: 960px) {
        .card-grid--three {
          grid-template-columns: repeat(3, minmax(0, 1fr));
        }
      }

      .tag {
        display: inline-flex;
        align-items: center;
        gap: 0.35rem;
        padding: 0.4rem 0.8rem;
        border-radius: 999px;
        font-size: 0.78rem;
        font-weight: 600;
        background: rgba(76, 62, 237, 0.12);
        color: var(--brand-primary);
        letter-spacing: 0.02em;
      }

      .challenge-grid {
        display: grid;
        gap: 1.25rem;
      }

      @media (min-width: 900px) {
        .challenge-grid {
          grid-template-columns: repeat(2, minmax(0, 1fr));
        }
      }

      .challenge {
        display: grid;
        gap: 1rem;
        padding: 1.8rem;
        border-radius: var(--radius-lg);
        background: var(--surface);
        border: 1px solid var(--border);
        box-shadow: 0 18px 45px rgba(50, 40, 150, 0.08);
      }

      .challenge__heading {
        display: flex;
        align-items: baseline;
        justify-content: space-between;
        gap: 1rem;
      }

      .challenge__number {
        font-family: "Instrument Serif", serif;
        font-size: 2.8rem;
        color: var(--brand-primary);
      }

      .challenge__label {
        font-weight: 700;
        text-transform: uppercase;
        letter-spacing: 0.16em;
        font-size: 0.78rem;
        color: var(--text-soft);
      }

      .challenge__content {
        display: grid;
        gap: 0.75rem;
      }

      .challenge__block {
        display: grid;
        gap: 0.35rem;
      }

      .challenge__block strong {
        color: var(--text-strong);
      }

      .metrics {
        display: grid;
        gap: 1.2rem;
        grid-template-columns: repeat(auto-fit, minmax(180px, 1fr));
      }

      .metric {
        padding: 1.8rem;
        border-radius: var(--radius-md);
        background: rgba(255, 255, 255, 0.9);
        border: 1px solid rgba(255, 255, 255, 0.4);
        box-shadow: 0 24px 45px rgba(76, 62, 237, 0.12);
        display: grid;
        gap: 0.4rem;
        text-align: center;
      }

      .metric strong {
        font-size: clamp(2rem, 4vw, 2.6rem);
        color: #fff;
      }

      .metric span {
        color: rgba(255, 255, 255, 0.85);
      }

      .timeline {
        position: relative;
        display: grid;
        gap: 1.5rem;
        padding-left: 1.4rem;
      }

      .timeline::before {
        content: "";
        position: absolute;
        left: 0.5rem;
        top: 0;
        bottom: 0;
        width: 2px;
        background: linear-gradient(180deg, var(--brand-primary), var(--brand-secondary));
      }

      .timeline__item {
        position: relative;
        background: var(--surface);
        border-radius: var(--radius-md);
        padding: 1.7rem 1.9rem;
        border: 1px solid var(--border);
        box-shadow: var(--shadow-md);
      }

      .timeline__item::before {
        content: attr(data-step);
        position: absolute;
        left: -1.4rem;
        top: 1.6rem;
        background: var(--brand-primary);
        color: #fff;
        border-radius: 999px;
        width: 2.3rem;
        height: 2.3rem;
        display: grid;
        place-items: center;
        font-weight: 700;
      }

      .steps {
        list-style: none;
        padding: 0;
        margin: 0;
        display: grid;
        gap: 1rem;
      }

      .steps li {
        background: rgba(255, 255, 255, 0.9);
        border-radius: 1rem;
        padding: 1.1rem 1.3rem;
        border: 1px solid rgba(76, 62, 237, 0.12);
        box-shadow: inset 0 1px 0 rgba(255, 255, 255, 0.6);
      }

      table {
        width: 100%;
        border-collapse: collapse;
        font-size: 0.95rem;
      }

      th,
      td {
        text-align: left;
        padding: 0.75rem 1rem;
        border-bottom: 1px solid rgba(110, 102, 170, 0.12);
      }

      thead th {
        font-weight: 600;
        color: var(--text-strong);
      }

      details {
        border-radius: var(--radius-md);
        border: 1px solid var(--border);
        padding: 1.4rem 1.6rem;
        background: var(--surface);
        box-shadow: var(--shadow-md);
      }

      summary {
        font-weight: 600;
        cursor: pointer;
        color: var(--text-strong);
      }

      details[open] {
        border-color: rgba(76, 62, 237, 0.3);
      }

      .testimonials {
        display: grid;
        gap: 1.5rem;
        grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
      }

      .testimonial {
        padding: 1.6rem 1.8rem;
        border-radius: var(--radius-md);
        background: var(--surface);
        border: 1px solid var(--border);
        box-shadow: var(--shadow-md);
      }

      blockquote {
        margin: 0 0 1rem;
        font-size: 1.05rem;
        line-height: 1.5;
        color: var(--text-strong);
      }

      .cta-band {
        background: linear-gradient(135deg, var(--brand-primary), #1f1a3d);
        color: rgba(255, 255, 255, 0.92);
        border-radius: calc(var(--radius-xl) * 0.9);
        padding: clamp(3rem, 5vw, 3.8rem);
        text-align: center;
        box-shadow: 0 40px 90px rgba(31, 26, 61, 0.35);
        display: grid;
        gap: 1.5rem;
      }

      footer {
        padding: 3.5rem 1.5rem;
        background: #120f2a;
        color: rgba(236, 233, 255, 0.75);
      }

      footer a {
        color: rgba(255, 255, 255, 0.9);
      }

      .footer__inner {
        max-width: var(--content-max);
        margin: 0 auto;
        display: grid;
        gap: 1.5rem;
      }

      .footer__meta {
        display: flex;
        flex-wrap: wrap;
        justify-content: space-between;
        gap: 1.5rem;
        align-items: center;
      }

      .footer__links {
        display: grid;
        gap: 0.85rem;
        list-style: none;
        padding: 0;
        margin: 0;
      }

      .footnote {
        font-size: 0.85rem;
        color: rgba(236, 233, 255, 0.65);
      }
    </style>
  </head>
  <body>
    <div class="page">
      <header class="navbar">
        <div class="navbar__inner">
          <a class="navbar__brand" href="#inicio">
            <span class="navbar__brand-mark">H</span>
            <span>Homason</span>
          </a>
          <nav aria-label="Principal">
            <ul class="navbar__menu">
              <li><a href="#modelo">Modelo Homason</a></li>
              <li><a href="#como-funciona">Cómo trabajamos</a></li>
              <li><a href="#planes">Planes</a></li>
              <li><a href="#materiales">Materiales</a></li>
              <li><a href="#faq">FAQ</a></li>
            </ul>
          </nav>
          <div class="navbar__actions" role="group" aria-label="Accesos rápidos">
            <a class="btn btn--ghost" href="<%= professionals_dashboard_path %>">Dashboard profesionales</a>
            <a class="btn btn--ghost" href="<%= clients_dashboard_path %>">Dashboard clientes</a>
            <a class="btn btn--outline" href="<%= professionals_signup_path %>">Nuevo profesional</a>
            <a class="btn btn--outline" href="<%= clients_signup_path %>">Nuevo cliente</a>
            <a class="btn btn--primary" href="<%= professionals_login_path %>">Nueva sesión profesional</a>
            <a class="btn btn--primary" href="<%= clients_login_path %>">Nueva sesión cliente</a>
            <a class="btn btn--primary" href="#contacto">Agenda una demo</a>
          </div>
        </div>
      </header>
      <main id="inicio">
        <%= yield %>
      </main>
      <footer>
        <div class="footer__inner" id="contacto">
          <div class="footer__meta">
            <div>
              <strong>Homason</strong><br />
              Construcción orquestada desde la estrategia hasta el cierre de obra.
            </div>
            <div>
              <a class="btn btn--ghost" href="mailto:hola@homason.com">hola@homason.com</a>
            </div>
          </div>
          <ul class="footer__links">
            <li>Teléfono: <a href="tel:+34911223344">+34 911 223 344</a></li>
            <li>Oficinas: Paseo de la Castellana 200, Madrid</li>
            <li>Horario de atención: Lunes a sábado, 9:00 – 20:00</li>
          </ul>
          <p class="footnote">&copy; <%= Time.current.year %> Homason. Todos los derechos reservados.</p>
        </div>
      </footer>
    </div>
  </body>
</html>