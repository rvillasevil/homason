<!DOCTYPE html>
<html lang="es">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width,initial-scale=1" />
    <title>Panel - Homason</title>
    <style>
      body { font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif; margin: 0; background: #f5f6f8; color: #1f2933; }
      header { background: #0f172a; color: white; padding: 1.5rem 2rem; display: flex; justify-content: space-between; align-items: center; }
      header h1 { margin: 0; font-size: 1.5rem; }
      nav a { color: white; margin-left: 1rem; text-decoration: none; font-weight: 500; }
      main { padding: 2rem; }
      .grid { display: grid; gap: 1.5rem; }
      .two-columns { grid-template-columns: repeat(auto-fit, minmax(320px, 1fr)); }
      .card { background: white; border-radius: 12px; padding: 1.5rem; box-shadow: 0 10px 25px -15px rgba(15, 23, 42, 0.45); }
      .card h2 { margin-top: 0; font-size: 1.2rem; color: #0f172a; }
      table { width: 100%; border-collapse: collapse; font-size: 0.95rem; }
      th, td { padding: 0.5rem; text-align: left; border-bottom: 1px solid #e2e8f0; }
      th { text-transform: uppercase; font-size: 0.75rem; letter-spacing: 0.08em; color: #64748b; }
      .status { display: inline-flex; align-items: center; padding: 0.2rem 0.6rem; border-radius: 999px; font-size: 0.75rem; font-weight: 600; text-transform: uppercase; }
      .status[data-status="pending"] { background: #fef3c7; color: #92400e; }
      .status[data-status="confirmed"] { background: #dcfce7; color: #166534; }
      .status[data-status="completed"] { background: #dbeafe; color: #1d4ed8; }
      .status[data-status="cancelled"] { background: #fee2e2; color: #b91c1c; }
      .muted { color: #64748b; font-size: 0.85rem; }
      .attachments { display: flex; flex-wrap: wrap; gap: 0.5rem; }
      .attachments a { background: #0f172a; color: white; padding: 0.3rem 0.8rem; border-radius: 999px; text-decoration: none; font-size: 0.8rem; }
      footer { text-align: center; padding: 1rem 0; color: #94a3b8; font-size: 0.8rem; }
    </style>
  </head>
  <body>
    <header>
      <h1>Homason · Panel</h1>
      <nav>
        <% if current_user&.professional? %>
          <a href="<%= professionals_dashboard_path %>">Panel profesional</a>
        <% end %>
        <% if current_user&.customer? %>
          <a href="<%= clients_dashboard_path %>">Panel cliente</a>
        <% end %>
      </nav>
    </header>
    <main>
      <%= yield %>
    </main>
    <footer>
      Gestión integral de reservas, pedidos y leads.
    </footer>
  </body>
</html>